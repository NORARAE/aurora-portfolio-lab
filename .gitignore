# --- Secrets (NEVER commit) ---
.streamlit/secrets.toml
secrets.toml
.env
*.key

# --- Local runtime state (per-user, not source) ---
aurora.db

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# --- Virtual environments ---
.venv/
venv/
env/

# --- OS / editor cruft ---
.DS_Store
.vscode/
.idea/

# NOTE: .streamlit/config.toml (theme) IS committed on purpose — it's part of
# the app's look. Only .streamlit/secrets.toml is ignored above.
